define host {{
    use        {use}
    host_name  {host_name}
    alias      {alias}
    address    {address}
}}

define hostextinfo {{
    host_name  {host_name}
    notes_url https://tls.homeinfo.de/termgr/mgr?cid={cid}&tid={tid}%&action=details
    icon_image {icon}
}}
